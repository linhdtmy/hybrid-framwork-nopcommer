package pageObject.user.nopcommerce;

import org.openqa.selenium.WebDriver;

import common.BasePage;

public class BackUserObject extends BasePage{
	private WebDriver driver;

	public BackUserObject(WebDriver driver) {
		this.driver = driver;
	}
}
